<% if(alcoholList) { %>
  <% alcoholList.forEach(show => { %>

    <div class="container">
      <h4 class="item-name"><%= show.name %></h4>
      <div class="price">Price: <%= show.price %></div>
      <p class="item-description">Type: <%= show.description %> </p>
    </div>

 <% }) %>

<% } %> 